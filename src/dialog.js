var React = require('react');
var Dialog;
Dialog = React.createClass({
    render: function () {
        return (
            <div className="Dialog">
                Dialog
            </div>
        );
    }
});

module.exports = Dialog;